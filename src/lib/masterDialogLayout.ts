export const MASTER_EDIT_DIALOG_WIDTHS = {
    compact: 'min(520px, 96vw)',
    medium: 'min(640px, 96vw)',
    standard: 'min(720px, 96vw)',
    wide: 'min(980px, 96vw)'
} as const;

export const MASTER_DETAIL_DIALOG_WIDTHS = {
    compact: 'min(520px, 96vw)',
    medium: 'min(640px, 96vw)',
    standard: 'min(720px, 96vw)',
    wide: 'min(900px, 96vw)'
} as const;
